meta {
  name: List TransferProcesses
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/ui/service-providers/{{providerId}}/tenants/{{tenant_id}}/participants/{{participant_id}}/transfer-processes
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
