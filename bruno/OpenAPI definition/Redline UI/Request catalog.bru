meta {
  name: Request catalog
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/ui/service-providers/{{providerId}}/tenants/{{tenant_id}}/participants/{{participant_id}}/catalog?counterPartyIdentifier=did:web:foobar
  body: json
  auth: inherit
}

params:query {
  counterPartyIdentifier: did:web:foobar
  ~cacheControl: 
}

headers {
  ~Cache-Control: 
}
